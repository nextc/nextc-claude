# OS
.DS_Store

# Claude Code — ignore personal files, commit the rest
.claude/settings.local.json
.claude/.mcp.json

# Reference/vendor OSS code
reference-oss/

# Internal specs (not shipped with plugin)
specs/
