import 'package:{{PACKAGE_NAME}}/shared/models/{{ENTITY_SNAKE}}.dart';
import 'package:{{PACKAGE_NAME}}/core/utils/paginated_response.dart';

/// Repository interface for [{{ENTITY_PASCAL}}] entities.
///
/// Concrete implementations handle storage details (Supabase, API, local DB).
/// Business logic depends on this interface, never on the implementation.
abstract class {{ENTITY_PASCAL}}Repository {
  /// Fetch a paginated list of {{ENTITY_SNAKE}}s.
  Future<PaginatedResponse<{{ENTITY_PASCAL}}>> findAll({
    String? cursor,
    int limit = 20,
  });

  /// Fetch a single {{ENTITY_SNAKE}} by ID. Returns null if not found.
  Future<{{ENTITY_PASCAL}}?> findById(String id);

  /// Create a new {{ENTITY_SNAKE}}. Returns the created entity.
  Future<{{ENTITY_PASCAL}}> create({{ENTITY_PASCAL}} entity);

  /// Update an existing {{ENTITY_SNAKE}}. Returns the updated entity.
  Future<{{ENTITY_PASCAL}}> update({{ENTITY_PASCAL}} entity);

  /// Delete a {{ENTITY_SNAKE}} by ID.
  Future<void> delete(String id);
}
