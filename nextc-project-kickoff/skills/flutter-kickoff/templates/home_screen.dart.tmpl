import 'package:flutter/material.dart';
{{#RIVERPOD}}
import 'package:flutter_riverpod/flutter_riverpod.dart';
{{/RIVERPOD}}
{{#GO_ROUTER}}
import 'package:go_router/go_router.dart';
{{/GO_ROUTER}}
{{#AUTO_ROUTE}}
import 'package:auto_route/auto_route.dart';
import 'package:{{PACKAGE_NAME}}/core/router/app_router.dart';
{{/AUTO_ROUTE}}

/// Welcome screen for {{PRODUCT_NAME}}.
///
/// Shows the product name, tagline, and navigation cards for each MVP feature.
/// First `flutter run` shows something that looks like your app, not boilerplate.
{{#AUTO_ROUTE}}
@RoutePage()
{{/AUTO_ROUTE}}
{{#RIVERPOD}}
class HomeScreen extends ConsumerWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
{{/RIVERPOD}}
{{^RIVERPOD}}
class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
{{/RIVERPOD}}
    final theme = Theme.of(context);

    return Scaffold(
      body: SafeArea(
        child: SingleChildScrollView(
          padding: const EdgeInsets.all(24),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              const SizedBox(height: 48),
              // Hero product name.
              Text(
                {{PRODUCT_NAME_ESCAPED}},
                style: theme.textTheme.displaySmall?.copyWith(
                  fontWeight: FontWeight.bold,
                ),
              ),
              const SizedBox(height: 8),
              // Tagline.
              Text(
                {{TAGLINE_ESCAPED}},
                style: theme.textTheme.titleMedium?.copyWith(
                  color: theme.colorScheme.onSurfaceVariant,
                ),
              ),
              const SizedBox(height: 48),
              // Feature navigation cards.
              //
              // Example card (scaffolder generates one per MVP feature):
              // _FeatureCard(
              //   title: 'Rooms',
              //   description: 'Create and join collaborative rooms',
              //   icon: Icons.meeting_room_outlined,
              //   route: '/rooms',
              //   isCore: true,
              // ),
{{FEATURE_CARDS}}
            ],
          ),
        ),
      ),
    );
  }
}

class _FeatureCard extends StatelessWidget {
  const _FeatureCard({
    required this.title,
    required this.description,
    required this.icon,
    required this.route,
    this.isCore = false,
  });

  final String title;
  final String description;
  final IconData icon;
  final String route;
  final bool isCore;

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);

    return Card(
      elevation: isCore ? 2 : 0,
      color: isCore
          ? theme.colorScheme.primaryContainer
          : theme.colorScheme.surfaceContainerLow,
      child: InkWell(
{{#GO_ROUTER}}
        onTap: () => context.go(route),
{{/GO_ROUTER}}
{{#AUTO_ROUTE}}
        onTap: () => context.router.pushNamed(route),
{{/AUTO_ROUTE}}
        borderRadius: BorderRadius.circular(12),
        child: Padding(
          padding: const EdgeInsets.all(20),
          child: Row(
            children: [
              Icon(
                icon,
                size: 32,
                color: isCore
                    ? theme.colorScheme.onPrimaryContainer
                    : theme.colorScheme.onSurfaceVariant,
              ),
              const SizedBox(width: 16),
              Expanded(
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    Text(
                      title,
                      style: theme.textTheme.titleMedium?.copyWith(
                        fontWeight: FontWeight.w600,
                      ),
                    ),
                    const SizedBox(height: 4),
                    Text(
                      description,
                      style: theme.textTheme.bodySmall?.copyWith(
                        color: theme.colorScheme.onSurfaceVariant,
                      ),
                    ),
                  ],
                ),
              ),
              const Icon(Icons.chevron_right),
            ],
          ),
        ),
      ),
    );
  }
}
