import 'package:{{PACKAGE_NAME}}/core/utils/paginated_response.dart';
import 'package:{{PACKAGE_NAME}}/features/{{FEATURE_SNAKE}}/domain/{{ENTITY_SNAKE}}_repository.dart';
import 'package:{{PACKAGE_NAME}}/shared/models/{{ENTITY_SNAKE}}.dart';

/// Concrete [{{ENTITY_PASCAL}}Repository] implementation.
///
/// All methods throw [UnimplementedError] — wire your data source
/// (Supabase client, REST API, local DB) in `/feature-dev`.
class {{ENTITY_PASCAL}}RepositoryImpl implements {{ENTITY_PASCAL}}Repository {
  // TODO: inject data source (Supabase client, API client, etc.)

  @override
  Future<PaginatedResponse<{{ENTITY_PASCAL}}>> findAll({
    String? cursor,
    int limit = 20,
  }) =>
      throw UnimplementedError();

  @override
  Future<{{ENTITY_PASCAL}}?> findById(String id) =>
      throw UnimplementedError();

  @override
  Future<{{ENTITY_PASCAL}}> create({{ENTITY_PASCAL}} entity) =>
      throw UnimplementedError();

  @override
  Future<{{ENTITY_PASCAL}}> update({{ENTITY_PASCAL}} entity) =>
      throw UnimplementedError();

  @override
  Future<void> delete(String id) => throw UnimplementedError();
}
